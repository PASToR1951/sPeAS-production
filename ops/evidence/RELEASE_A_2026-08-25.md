# PeAS Release A deployment evidence — 2026-08-25

## Scope and isolation

- Production guest: Proxmox VM `304` (`app-04`, `peas.spud.edu.ph`).
- Guest address: `192.168.2.104`.
- Shared Proxmox host, storage, networking, and VMs `300`–`303` and `305`
  were not changed.
- Repository base commit: `2886070`; Release A is deployed from the reviewed
  working tree in `C:\Users\peas\Desktop\sPeAS-production`.

## Configuration and restart

- Release A uses `PEAS_CSP_MODE=report-only`.
- Better Auth canonical/trusted origin: `https://peas.spud.edu.ph`.
- Trusted proxy: `192.168.2.3` only; the campus VM subnet is not trusted.
  The direct peer was measured on 2026-08-26 while sending repeated requests
  through `https://peas.spud.edu.ph`; the earlier provisional gateway value
  `192.168.2.1` was incorrect and must not be used for proxy trust or firewall
  authorization.
- Native listener: `192.168.2.104:80` (the existing shared-nginx upstream
  port was preserved).
- `security.txt` uses the pre-existing operations mailbox; its address is not
  copied into this evidence file.
- Previous environment backup:
  `C:\ProgramData\PeAS\config\peas.env.pre-release-a.20260825-090029.bak`.
- Privileged restart log:
  `C:\ProgramData\PeAS\logs\release-a-elevated-restart.log`.
- Startup report:
  `C:\ProgramData\PeAS\logs\startup-reports\peas-startup-20260825-093447-18324.txt`.
- `PeAS-Boot-Daemon` was registered as SYSTEM and started successfully.
- Old supervisor/web PIDs: `2140` / `10116`.
- New supervisor/web PIDs at acceptance: `18324` / `17592`.
- Database-backed readiness became healthy at 09:32 PHT and the supervisor's
  120-second sustained-readiness gate passed at 09:34 PHT.

The standard supervisor found and applied the previously pending guarded
migration `0008_remove_document_access_requests.sql`. Its PII guard did not
block, and the migration ledger reports no remaining pending migration. This
Release A author/header change itself did not introduce a new schema migration.

## Live verification

| Check | Result |
| --- | --- |
| HTTP to canonical HTTPS redirect | Passed (`301`) |
| TLS certificate lifetime | Passed (`71.1` days remaining) |
| Explicit private listener | Passed (`192.168.2.104:80`) |
| Liveness and database readiness | Passed (`200`) |
| Readiness `Cache-Control: no-store` | Passed |
| HTML, JSON, JSON error, and PDF application security headers | Passed |
| CSP report-only core policy | Passed |
| Unauthenticated `/api/authors/all` | Passed (`401`) |
| Unauthenticated `/api/document-authors/2` | Passed (`401`) |
| Unauthenticated deprecated alias | Passed (`401`) |
| Guest/public author DTO | Passed (only `id`, `full_name`) |
| Nonexistent guest author document | Passed (`404`) |
| Homepage `stats.totalAuthors` and rendered AUTHORS count | Passed |
| `/ping` minimized and deprecated | Passed |
| `/.well-known/security.txt` | Passed |
| Source-map probe | Passed (`404`) |
| TRACE rejection | Passed (`405`) |
| Public document 2 metadata and author preview | Passed in browser |
| Public PDF attachment, `no-store`, and `nosniff` | Passed |
| HSTS at shared nginx edge | **Failed: header absent** |

The repository verifier remains red because HSTS is intentionally an ingress
gate and the shared nginx host was outside this guest-only change. Application
checks were then run individually to avoid representing the edge as accepted.

## Open production gates

- Render, validate, and reload the PeAS nginx virtual host with the reviewed
  HSTS/rate-limit/forwarding-header template. This must be coordinated with the
  shared-ingress owner and must not change other virtual hosts.
- Apply the reviewed Windows Firewall rule for VM 304 after confirming the
  nginx source and any distinct monitoring source. No broad subnet allow is
  acceptable.
- Repair SMTP authentication. The startup report was written locally, but the
  operations email returned SMTP `5.7.0 Authentication Required`.
- Complete authenticated administrator author-picker, upload, review, media,
  and PDF workflow validation.
- Observe CSP reports for seven complete days before Release B enforcement.
- Complete the two-repository backup and isolated recovery qualification.
- Attach green CI/tagged-release URLs and an all-green external verifier result
  before production sign-off.
